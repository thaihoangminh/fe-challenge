import { Currency } from 'components/asset-item/asset-item.type';

type ActiveModalState = 'successfully-send' | 'select-assets' | '';

type ActiveCurrency = Currency;

export type { ActiveModalState, ActiveCurrency };
